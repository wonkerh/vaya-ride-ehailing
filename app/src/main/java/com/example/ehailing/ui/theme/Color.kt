package com.example.ehailing.ui.theme

import androidx.compose.ui.graphics.Color

// Base
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)

// Surfaces
val SurfaceLight = Color(0xFFF6F6F6)   // cards, inputs, bottom sheets
val BorderLight  = Color(0xFFE2E2E2)   // 1px borders

// Text
val TextPrimary   = Color(0xFF000000)
val TextSecondary = Color(0xFF6B6B6B)   // grey caption
val TextHint      = Color(0xFF9E9E9E)   // placeholder

// Accent
val AccentRed = Color(0xFFE11900)       // cancel / error only

// Map fallback (used before style JSON loads)
val MapBackground = Color(0xFFF2F2F2)